version: '3'

services:
  cf-module:
    build:
      context: .
      dockerfile: Dockerfile

    restart: always
    
    ports:
      - 50051:50051

    networks:
      - embers


networks:
  embers:
    driver: bridge  